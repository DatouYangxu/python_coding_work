字符串可理解为不可修改的列表，所含元素为列表
运算优先级not>and>or
if。。。elif。。。else分支多个语句块最多会执行一个
numpy库
1.arrange函数：类似于range，可生成等差数列，不局限于整数
2.linspace[a,b,n]：将区间（a,b)等分为n个点，return 类型为np特有的数组，这n个点的序列
3.array（n）：将n转化为数组类型

ALU 运算器
CU	控制器
更小更快的缓存与大容量低速的内存组合使用
提高缓存命中率的原理：
局部性原理 ——CPU在一段时间内对主存的访问总是局限于主存的某个较小的区域中